pipeline {
    agent any
    environment {
        KEY_TYPE = "rsa"
    }
    stages {
        stage('first') {
            steps {
                echo "The key type is ${KEY_TYPE}"
                echo "The PATH is ${PATH}"
            }
        }
        stage('second') {
            steps {
                echo "The key type is ${KEY_TYPE}"
            }
        }
    }
}
