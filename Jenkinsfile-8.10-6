pipeline {
    agent any
    stages {
        stage('first') {
            environment {
                CRED = credentials("eedd4684-7c27-4721-ba2a-684734f117d6")
            }
            steps {
                sh 'echo "The username is ${CRED_USR}"'
                sh 'echo "The password is ${CRED_PSW}"'
            }
        }
    }
}
