pipeline {
    agent {
        docker { dockerfile true }
    }
    stages {
        stage('test') {
            steps {
        			sh 'go version'
        			sh 'curl --version'
            }
        }
    }
}
