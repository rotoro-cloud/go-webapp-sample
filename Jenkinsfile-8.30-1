pipeline {
    agent any
    options {
        retry(3)
    }
    stages {
        stage('first') {
            steps {
                sh "echo Now: \$(date '+%Y-%m-%d %H:%M:%S')"
                sleep 1
            }
        }
        stage('second') {
            steps {
                sh "echo Now: \$(date '+%Y-%m-%d %H:%M:%S')"
                sleep 1
            }
        }
    }
}
