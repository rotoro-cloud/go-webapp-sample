pipeline {
    agent any



    stages {
        stage('first') {
            environment {
                KEY_TYPE = "rsa"
            }
            steps {
                echo "The key type is ${KEY_TYPE}"
            }
        }
        stage('second') {
            steps {
                echo "The key type is ${KEY_TYPE}"
            }
        }
    }
}
